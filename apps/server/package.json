{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@testproj/api": "workspace:*",
    "@trpc/server": "catalog:",
    "califi": "^1.0.0",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "hono-rate-limiter": "^0.4.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "^1.2.6",
    "tsdown": "catalog:",
    "@testproj/config": "workspace:*"
  }
}
